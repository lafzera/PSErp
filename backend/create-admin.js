const { PrismaClient } = require("@prisma/client"); const bcrypt = require("bcryptjs"); const prisma = new PrismaClient(); async function createAdmin() { try { const hashedPassword = await bcrypt.hash("admin123", 10); const user = await prisma.user.create({ data: { email: "admin@admin.com", password: hashedPassword, name: "Administrador", role: "ADMIN" } }); console.log("Usuário admin criado:", user); } catch (error) { console.error("Erro:", error); } finally { await prisma.$disconnect(); } } createAdmin();
